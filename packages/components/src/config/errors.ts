export default Object.freeze({
  allowClearAndIcon:
    "Only one icon can be displayed in the input field. Either one provided or the clear icon, if allowClear=true",
  floatingPlaceholderAndLabel: "Cannot set a value for the label and the property floatingPlaceholder=true",
});
